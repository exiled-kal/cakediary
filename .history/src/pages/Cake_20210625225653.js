const Cake = ({setSelectedImg, selectedImg}) => {

    const handleClick = e => {
        if(e.target.classList.contains)
    }
    return (
        <div>
            
        </div>
    )
}

export default Cake
