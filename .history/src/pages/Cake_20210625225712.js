const Cake = ({setSelectedImg, selectedImg}) => {

    const handleClick = e => {
        if(e.target.classList.contains('backdrop')) {
            setSelected
        }
    }
    return (
        <div>
            
        </div>
    )
}

export default Cake
