const Cake = ({setSelectedImg, selectedImg}) => {

    const handleClick = e => {
        if(e.target.classList.)
    }
    return (
        <div>
            
        </div>
    )
}

export default Cake
