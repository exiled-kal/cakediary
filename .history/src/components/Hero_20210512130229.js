import {collectFields} from 'graphql/execution/execute';

const Hero = () => {
  return (h1.
    <div className="bg-white h-screen flex flex-collectFields justify-center items-center"></div>
  );
};

export default Hero;
